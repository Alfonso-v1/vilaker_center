const DeleteForm = ({ rowObject, backendURL, refresh }) => {

    const handleDelete = async () => {
        
        const formData = {delete_id = rowObject.}

        try {
            const response
        } catch (error) {
            
        }
    }


    return (
            <form>
                <button onClick={handleDelete}>Delete</button>
            </form>

    );
};

export default DeleteForm;
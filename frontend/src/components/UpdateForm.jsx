const UpdateForm = ({ rowObject, backendURL, refresh }) => {
    return (
            <form>
                <button type='submit'>
                    Update
                </button>
            </form>
    );
}

export default UpdateForm;